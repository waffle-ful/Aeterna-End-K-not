# 公式サーバーの仕組みと「Hackingキック」についての観測メモ

AmongUsの公式サーバーは、モッドホストを`reason:Hacking`で切断することがあります。
このメモは、長期間の運用でその切断とサーバーの挙動について観測した事実をまとめたものです。
Innerslothは内部仕様を公開していないので、以下には**観測からの推定**が混ざります。

## 1.公式サーバーは中継器だが、中身の一部を覚えている

AmongUsのゲーム計算はサーバーでなく**ホストのクライアント**が担います。
公式サーバーはホストのパケットを他プレイヤーへ**転送する**中継器です。

そのため:

- 誰が死んだか、どちらが勝ったかといったルール判定を、サーバーは行わない
- ゲーム内にオブジェクトが何個あっても、サーバーの負荷は増えない。
負荷と制限の対象は**送信した瞬間**だけ
- 会議明けのように大量のオブジェクトを一斉に生成・再同期すると、
溜まっていた分が一度にパケットになり、その瞬間だけ送信が跳ね上がる

⚠️2026-08-01に「サーバーは中身を何も知らない」という記述を訂正しました。
後述の実験で、サーバーが少なくとも
**「このセッションに属するオブジェクトの一覧」と「どのクライアントがどのプレイヤーか」**を
保持していると分かりました。サーバーはパケットを中継しつつ、**プレイヤーの同一性に関わる部分を自前で記憶し、
そこに触るパケットを転送せず切断で応じます**。

## 2.「キック」と「BAN」は別のシステムに見える

観測した範囲では、公式サーバー側の制裁は少なくとも次の系統に分かれます:

|系統|見え方|記録性|
|---|---|---|
|①トランスポート層の保護|`reason:Hacking`で即切断。チャット告知なし|**残らない**(何度食らってもBANに繋がらない)|
|②ゲーム内アンチチート|「○○はInnerslothのアンチチートによりキックされました」と全員に告知|BAN段階に繋がる可能性|
|③モデレーション(通報・チャット)|ヘイトスピーチ等での処罰|BANあり|
|④マッチメイキング制裁|途中抜け等の時限アクセス禁止(例:1時間)|時限BAN|

根拠:

- `Hacking`キックを長期間に何十回受けても、アカウント制裁(時限BAN含む)は一度も来ていない
→①は接続単位のステートレスな保護で、アカウントに記録を残さないと考えられる
- ②のキックには専用のチャット告知が出る。①で告知が出たことはない
- `Hacking`は汎用ラベルで、複数の検知機序が同じ表示を共有している

## 3.`Hacking`キックは送信量で決まらない

同一セッション内で比べると、**送信本数もバイト数も明らかに多い時間帯が生き残り、少ない時間帯がキックされた**
事例が複数あります。パケットの本数・総バイト数・密度・構成のどれを見ても、キックの有無と対応しませんでした。
これで単純なレート制限説は否定できます。

**1パケットに詰めたメッセージの個数**も無関係でした。クライアントには人数に比例して増える自主規制上限がありますが、
それを**大幅に超えて詰めてもキックは一度も起きていません**。サーバーはこの上限を検査していないと見ています。

(ここで否定したのは「負荷に応じて変動するレート制限」です。§4-3のとおり、
**固定の本数を決定論的に数える**別系統のルールは、その後の実験で見つかりました)

## 4.キックを決めているのは内容のルール

2026年7月末の切り分け実験で、決定的な対照が取れました。

**大きさも構造も同じパケットで、更新対象のオブジェクトだけを差し替えた**結果:

- ホスト自身のプレイヤー情報を更新するパケットは、**ごく小さな1通でも即キック**
- 対象をモッドの独自オブジェクトに替えたパケットは、**同じ形のまま無傷**

①の正体は、**パケットの内容を照合する小さなルール表**と考えています。
「自分のプレイヤーデータを詐称するな」の類のチェックがあり、そこに触れるとサイズやレートに関係なく
決定論的に切られる。観測にはこの読みがいちばんよく合います。

### 4-1.2026年8月の追加実験でルールの輪郭が固まった

同じ手法で18通りの送り方を試し、**プレイヤーの「同一性」に触る操作だけが禁止されている**
ことが分かりました。禁止されている操作は次の6つです。いずれも11〜86バイトの極小パケットで
**100%切断**され、揺らぎはありません。8月に立てた5行目は2026-09-16に取り下げ、
同じ日に見つけた2つを加えています(どちらも後述)。

|禁止されている操作|補足|
|---|---|
|生きているプレイヤーの**本体オブジェクトを更新**する|自分のぶんでも他人のぶんでも(§4の対照実験)|
|**接続中のプレイヤーの持ち物として新しいオブジェクトを作る**|中身を送る前、作った時点で切断|
|**セッションに登録済みの番号を、新しいオブジェクトとして名乗り直す**|プレイヤーの4つのオブジェクトも、システム側のオブジェクトも該当|
|生きているプレイヤーの**本体または人物データを消す**|見た目の更新は自由だが、消すのは禁止|
|複数の通知をまとめる**「包み」の中に、全員向けの通知を入れる**|**中身も対象も問わない**。空の通知でも18バイトで切断(2026-09-16)|
|**通知を1つも入れずに「包み」だけ送る**|**11バイト**で切断。通知を1つでも入れれば無傷(2026-09-16)|

次の操作はどれも**無傷**でした(上の各行と1か所だけ違う対照を取っています):

- モッドの独自オブジェクトは、更新・作成・削除とも自由
- **存在しないプレイヤーの持ち物**としてオブジェクトを作るのは自由(判定は「接続中かどうか」だけ)
- プレイヤーの**位置情報**と**見た目情報**の更新は自由(867バイトでも通る)
- 新しいオブジェクトが実在プレイヤーと**同じ番号を名乗る**こと自体は自由
- **全員向けの通知を包みに入れず送る**のは自由(存在しない番号宛でも、更新でも削除でも)

サーバーが守っているのは**「誰がどのプレイヤーか」の対応表**です。
見た目や位置は申告制で書き換えられますが、**同一性を作る・付け替える・消す**操作は
サイズもレートも関係なく拒否されます。乗っ取りを防ぐ最小限の保護と読めます。

下2行は、触った対象でなく**パケットの組み立て方**で判定しています。どちらもバニラが
使わない形なので実害はありませんでした。それでも、対象さえ正しければ何を送っても通るわけではありません。
最後の行は、11バイト・通知ゼロ・対象ゼロのパケットでも切られます。中身が空でも安全とは言えません。

#### 補足(2026-08-01追記):禁止の意味は「他人の登録を騙るな」

表の2行目と3行目を字義どおりに読むと、矛盾が出ます。ホストは誰かが入室するたびに、
その人のオブジェクト一式を「接続中クライアントの持ち物」として正規に生成し、配信し直しているからです
(字義どおりなら、バニラのロビーに誰も入れません)。実際の通信を突き合わせると、
入室のたびにこの形のパケットが大量に流れていて、キックはゼロでした。

正しくは「**対応表と一致する正規の登録は合法。別のオブジェクトが接続中クライアントの持ち物を騙ったり、
既存の番号を横取りして宣言したりするのが違法**」です。上のまとめとも矛盾しません。

「全員向けでなく宛先を1人に指定して送れば検査を抜けるのでは」という仮説も試しました。
**宛先指定でも内容ルールは同じように発火し、即切断**されました。
送り方で結果が分かれたのは表の1行だけで、長いあいだその行を
「存在しない番号に全体通知で触るのは違法」と読んでいました。

#### 補足(2026-09-16追記):その行を取り下げ、本当の境界を加えた

その行を詰めていくと、**測り方の側に誤り**がありました。

検証に使っていた「全体通知」は、正確には
**複数の通知をまとめる「包み」の中に、全員向けの通知を入れた形**でした。
2026-09-16にこの包みへ**中身のない全員向け通知(18バイト)**を入れて送ると、
それだけで即切断されました。**対象の番号も通知の中身も入っていません**。
同じ包みに宛先指定の通知を入れた形(21バイト)は無傷で、
違いは**包みに入れた通知の種類**1点だけです。

|送った形|大きさ|結果|
|---|---|---|
|包み{全員向け通知(中身なし)}|18バイト|**即切断**|
|包み{全員向け通知(存在しない番号への更新)}|24バイト|**即切断**|
|包み{全員向け通知(存在する番号への更新)}|23バイト|**即切断**|
|包み{全員向け通知(自分の位置更新・本番で常時流している対象)}|29バイト|**即切断**|
|包み{宛先指定の通知(中身なし)}|21バイト|無傷|
|包み{宛先指定の通知(自分の位置更新)}|32バイト|無傷|

サーバーは**包みの中に全員向けの通知があること**で切っていて、
番号の有無は判定に入っていませんでした。バニラもこのモッドも、
全員向けの通知は**包みに入れず**送り、包みには宛先指定の通知だけを入れます。
この形を送ったクライアントがそれまでなかったので、ルールも表に出ませんでした。

**包みを外した全体通知**を撃ち直すと、同じ試合の中で次の結果になりました。

|送った形|大きさ|結果|
|---|---|---|
|全員向け通知(中身なし)|10バイト|無傷|
|全員向け通知(自分の位置更新)|21バイト|無傷|
|全員向け通知(**存在しない番号**への更新)|16バイト|無傷|
|全員向け通知(**存在しない番号**への削除。元の5行目と同じ形)|15バイト|無傷|
|(対照)自分の本体オブジェクトへの更新|26バイト|**即切断**|

同じ試合で対照が即座に切られているので、サーバーは送信を検査していました。
存在しない番号への全体通知は、削除でも更新でも合法です。**元の5行目は取り下げます。**

「送り方(全体通知か宛先指定か)で結果が変わるルール」という記述も取り下げます。
結果が変わるのは**包みに入れたとき**だけです。

**「包みの中に全員向けの通知を入れると切断」は、新しい禁止ルールとして表に残します**。
8月から1か月半「番号の存在が判定軸」と読み違えた原因は、比べた2つのパケットが送り方と
**包み方の両方で違っていた**ことです。

### 4-2.以前から輪郭がつかめていた2点

1.**実プレイヤーの「本体」オブジェクトへの更新は禁止**。自分のぶんでも他人のぶんでも、
極小サイズでも100%切断され、揺らぎはありません。
⚠️禁止対象はプレイヤー情報全般ではありません。名前・色・帽子などの見た目を持つ側のオブジェクトは、
通常のゲーム進行で絶えず更新されてもキックされません。**同じプレイヤーを構成するオブジェクトでも
種類ごとに扱いが違い**、保護されているのは片方だけです。

この2つは内部で隣り合う番号を持つ別オブジェクトなので、対照実験がきれいに取れました:

|更新した対象|サイズ|結果|
|---|---|---|
|自分の「本体」|25バイト|**即キック**|
|他プレイヤーの「本体」|25バイト|**即キック**|
|モッドの疑似プレイヤーの「本体」|25バイト|**無傷**|
|自分の「見た目情報」(本番で常時流れている側)|**866バイト**|**無傷**|

上3行は**中身もサイズも送り方も同一**で、触った対象だけが違います。34倍大きいパケットが
生き残った結果と合わせると、結果を決めるのは**どのオブジェクトを触ったか**だけで、
サイズとレートは関係しません。保護対象は実在プレイヤーの本体だけです。

2.**サイズ上限はパケットの種類ごとに違う**。一律1024バイトではありません。
名前表示の経路は800バイト前後で切られ、チャットは900バイト超でも通り、
画像データを載せた経路は800バイト台で通ります。

### 4-3.2026年8月の追加観測:「時間と数」を見る別系統のルール(2026-09-16に数値を改訂)

§4の内容ルールでは説明のつかないキック(中身は合法なのに切られる)が残っていました。
これも決定論的な条件として分離できました。(1)〜(3)は中身でなく**「いつ・いくつ」**で結果が決まります。
(4)は後に内容ルールの側と分かりましたが、記録として残します。

**(1)ゲーム開始直後の、新規オブジェクトの一斉生成(2026-09-16に発火しなくなった)**

2026年8月の実測では、ゲーム開始直後の十数秒の窓で新しいオブジェクトの生成通知を
**11本以上**まとめて送ると即切断されました。**10本なら無傷、11本で100%キック**で、
1本差で結果が反転する決定論的な境界です。**バイト列が同一**の束を、
窓が閉じた後(開始から十数秒後)に送ると無傷でした。そこから、
**開始直後だけ生成を厳しく検査する時間窓**があると読んでいました。ゲーム開始時はホストが
正規の初期化パケットを大量に送るので、そこを狙う攻撃への保護と考えられます。

2026-09-16に**同じ人数・本数・タイミングで撃ち直すと、無傷**でした。
8月に2回とも切断された11本でも、その試合で出せる最大の14本でも切られません。
人数を増やして20本以上をまとめて送っても同じです。同じ試合で内容ルール(§4-1)に触れる通知を
1通送ると即切断されるので、サーバーは送信を検査しています。

⇒ **2026年8月以降にサーバー側でこの境界を変えた(あるいは外した)**と見ています。
⚠️ 2026年7月末には、これで試合中のホストが実際に落ちました。いつ戻るか分からず、
避けても失うものはほとんどないので、**開始直後の一斉生成を避ける配慮は残しておく**ことを勧めます。

**(2)大人数への個別配信を続けると、「宛先の数」で切られる**

特定のプレイヤーにだけ見せるオブジェクトを人数ぶん個別に送る処理を数秒間続けると、
**宛先8人以下なら何度やっても無傷、10人以上なら数秒で切断**されました(9人は未測定)。
**1パケットの宛先を少数に分割しても結果は同じ**でした。
サーバーはパケットごとの宛先数でなく、**短い時間窓の中で触れた宛先の総数**を数えているようです。

「大人数ロビーでだけ落ちる」という長年の謎は、おそらくこれで説明できます。効いていたのはロビーの見かけの
人数でなく、**その瞬間に接続している宛先の数**です(途中退出者の多い試合では、大人数の
ロビーでも落ちません)。大人数へ一瞬触れただけでは切られず、数秒続けたときに切られるので、
持続・累積の成分もあります(閾値の正確な形は未特定)。

**(3)プレイヤーの強制移動(テレポート)は本数を数えられている**

ホストがプレイヤーの位置を強制的に動かす通知は、**数秒の窓に何本入るか**で判定されます。
2026-09-16に毎秒のペースを変えて撃ち直した結果です。

|毎秒のペース|結果|
|---|---|
|30本/秒|20秒間600本を撃ち切っても無傷|
|49本/秒|**20秒間980本を撃ち切っても無傷**|
|60本/秒|**355本目**(6.0秒後)で切断|
|73本/秒|**315本目**(4.4秒後)で切断|
|98本/秒|**352本目**(3.6秒後)で切断|

切断までの本数はペースによらず**330本前後**で、切断までの時間はペースに反比例します。
サーバーが数えているのは**窓の中の本数**で、毎秒の本数ではありません。

49本/秒が完走し60本/秒が切られるので、窓の長さは**約6秒**と挟めます
(6秒窓に49本/秒なら294本しか入らず、何秒撃っても閾値に届きません)。
実用上、**毎秒55本前後が撃ち続けられる上限**です。

切断までの本数は**同席人数にも依存しません**。毎秒100本で撃つと、2人のロビーで360本目、
3人のロビーで352本目に切れました。宛先が増えても閾値は下がりません。

⚠️ このメモには以前「窓は約10秒・毎秒50本でも358本目で死ぬ」と書いていました。
毎秒49本で980本を2回完走したので、これは誤りです。窓の長さを10秒から6秒に直すと、他の観測も全部説明がつきます。

この窓は**ラウンド全体の累計を数えていません**。先に200本撃ち、約1分空けて撃ち直すと、
2回目は単独で360本撃ってから切れました。1回目の本数は窓から抜けていました。

「テレポート系の役職が多い試合でホストだけ落ちる」現象は、おそらくこれで説明できます。
**この本数はホストのテレポート通知全体で共有**されていて、役職の能力のほか透明化の
明滅なども同じ枠を消費します。

**(4)中身が空のパケット(2026-09-16に決着:§4-1の内容ルール側だった)**

長い装飾名を送る処理の不具合で、**中身ゼロのパケット**が名前1件につき1本ずつ余計に出る
状態になったことがあります。送る内容も宛先も変わらないのに、
**一斉更新中のパケットが17本から41本(うち20本が空)に増え**、即切断されました。
空パケットが出ないように直すと、同じ長さの名前を同じ人数へ送っても無傷です。

当初は「空だから違法」か「本数が増えて(2)に触れた」かを切り分けられていませんでした。
**空のパケットを1本だけ撃つ実験**で決着しています。**1本・11バイトで即切断**です。
41本という数とは関係なく、1本目で条件を満たしていました。これは数のルールでなく
§4-1の内容ルールで、表の最終行にあたります。

(1)〜(3)は§3で否定した「負荷に応じて変動するレート制限」とは別物で、
**固定の数を数える決定論的なルール**です。1本差・2人差で結果が確実に反転するのはそのためです。
(3)では、**ペースを変えても切断までの本数がほぼ動かない**ことが、レートでなく本数を数えている証拠になりました。

⚠️ **この系統の数値は2026年8月から9月のあいだに変わっています**。
(1)は発火しなくなり、(3)は窓が約10秒から約6秒になりました。同じ期間、
内容のルール(§4〜§4-2)は変わっていません(今撃っても100%切られます)。
独立した2つの数値が同時に変わったことから、**数を数える側の値はサーバーの設定で、いつでも変わりうる**
と考えています。ここに書いた数字ぎりぎりまで攻める使い方は勧めません。

### 4-4.2026年9月の追加観測:1メッセージの大きさの境界と、超えたときの切れ方

§7のとおり、公式のmodding情報は**1メッセージの上限を1200バイト(ヘッダ込み)**と明記しています。
この境界をまたいで撃つと、**超え方によって切れ方が変わりました**。
どれも他の送信から10秒以上離して**1本だけ**撃った結果です。

|1本の大きさ|結果|
|---|---|
|903バイト|無傷。ロビー / 試合中 / 全員宛て / 特定の1人宛て / 他人の名前の書き換え、どの形でも|
|999バイト|無傷(ロビー・試合中の両方)|
|1200バイト|無傷(公式の上限ちょうど)|
|1500バイト|**ACKが返らない**。10回再送し、約7.5秒後に接続が切れる|
|2000バイト|`Hacking`で即切断|

1500バイトの切断理由は`Hacking`ではありません。画面には「サーバー接続が切れました」と出て、
本文に`Reliable packet ... was not ack'd after 7504ms (10 resends)`という生の文言が入ります。
サーバーはパケットを受け取って蹴ったのでなく、**受け取っていない**ように見えます。
上限を超えたパケットをサーバーが黙って捨て、こちらが再送を繰り返した末にタイムアウトで切れる形です。

キックに見えても、**実際には通信が成立していないだけ**のケースがあります。
切断理由のラベルを確認しないと、この2つを取り違えます。

## 5.「普段は通るのに稀に死ぬ」の正体は、たぶん観測不足

このメモには以前「蹴る対象は内容ベースだが、**発火閾値はサーバー側の瞬間負荷で変動する**」
という推定を書いていました。今回の実験結果は、この見方を支持しません。

- 違法なパターンは、極小サイズ1通でも**毎回100%**切られた
- 合法なパターンは、自主規制上限の数倍まで詰めても**一度も**切られなかった

サーバーの判定がランダムに揺れる様子は見つかりませんでした。
「同じことをしているのに普段は通り、稀に死ぬ」という長年の印象は、
**こちらの計測がキックを分ける変数を捉えていなかった**せいだと考えています。
本数やバイト数のメーターは、パケットの中身の形が変わっても数字が動かないので、
この種の違いを原理的に拾えません。

最近まで、同じ罠をもう1つ踏んでいました。「プレイヤー名が長いとキックされる」と考えて
名前の長さに厳しい上限を掛けていたのですが、根拠にした観測は**どれも会議明けの一斉更新**の最中で、
§4-3(2)の宛先数のルールが同時に効いていました。同じ長さの名前を
**静止状態で1本だけ**撃ち直すと無傷です。長さと同時本数を分離できていなかっただけで、
§4-4の上限のほかに長さのルールは見つかっていません。

§4-1の表から取り下げた行(存在しない番号への全体通知)も、同じ型の取り違えです。1つの条件を変えたつもりで、
実際には**送り方と包み方の2つを同時に変えていました**。包み方は実験道具の
既定値に埋まっていたので、比較表を何度作ってもその軸が表に出てきません。中身を全部抜いた
**空のパケット1本**を撃つまで、1か月半気づけませんでした。条件を1つだけ変えるときは、
送信されるバイト列の上でも差が1つだけかを先に確かめてください。

ランダムに見える現象の裏には、たいてい未観測の決定的な変数があります。
§4-3で見つけたルールがその例で、隠れていた変数は
「ゲーム開始からの経過時間」「その瞬間の宛先の数」「窓の中の通知の本数」でした。
どれも送信量のメーターには映りません。

3つ目がいちばん分かりやすい例です。長いあいだ「短時間に撃ちすぎると蹴られる」と考えて
毎秒レートを計測していましたが、サーバーが数えていたのは本数だったので、
レートのメーターからは境界が見えませんでした。**ペースを何段階か変えて、
切断までの本数がどれだけ動くかを比べた**ところ、本数がほぼ動かないことがすぐに分かりました。

## 6.それでもキックがゼロにならない理由

ルール表が決定論的でも、モッドホストは**バニラが送らない形のパケット**を送らざるをえません。
Innerslothはルールの全文を公開しておらず、予告なく変えることもあるので、未知の行に触れる可能性が残ります。
本モッドはそのために自動再ホストなどの復帰機構を備えています。
サーバー負荷への配慮として、モッド側で送信量も自主的に制限しています。

## 7.公式の一次資料

Innerslothは公式にmodding情報を公開しています:
https://github.com/Innersloth-LLC/AmongUsModdingInformation

- 1メッセージの上限は**1200バイト(ヘッダ込み)**
- 送信レートの公式値は未策定。バーストが必要ならmodding@innersloth.comへ相談する運用
- host-onlyモッドではアンチチートが緩和されるが、「常に無効化される保証はない」

パケットの挙動を推測する前に、この一次資料を確認してください。
