# 公式サーバーの仕組みと「Hacking キック」についての観測メモ

Among Us の公式サーバーと、モッドホストが時々食らう `reason: Hacking` 切断について、
長期間の運用で観測できた事実をまとめたメモです。あまり知られていない仕組みなので記録として残します。
(Innersloth の内部仕様は非公開のため、以下は**観測に基づく推定**を含みます)

## 1. 公式サーバーは「リレー(中継器)」である

Among Us はゲームの計算をサーバー上で行っていません。試合のシミュレーションは**ホストのクライアント**が行い、
公式サーバーはホストが送ったパケットを他プレイヤーへ**転送するだけ**の中継器です。

このため:

- サーバーはゲーム内に何が存在するかを把握していない (エンティティの状態を持たない)
- サーバーが監視・制限できるのは「**流れているパケットの本数・サイズ・中身**」だけ
- ゲーム内オブジェクトが何個「存在」していても、それ自体はサーバー負荷にならない。
  負荷・制限の対象になるのは**送信した瞬間**だけ (存在は無料、送信は有料)
- 逆に、大量のオブジェクトを一斉に生成・再同期する瞬間 (会議明けなど) は、
  蓄積していたものが一度にパケット化されるため、この瞬間だけ突出して危険になる

## 2. 「キック」と「BAN」は別のシステムに見える

観測できた範囲では、公式サーバー側の制裁は少なくとも次の系統に分かれているようです:

| 系統 | 見え方 | 記録性 |
|---|---|---|
| ① トランスポート層の保護 | `reason: Hacking` で即切断。チャット告知なし | **残らない** (何度食らっても BAN に繋がらない) |
| ② ゲーム内アンチチート | 「○○は Innersloth のアンチチートによりキックされました」と全員に告知 | BAN 段階に繋がる可能性 |
| ③ モデレーション (通報・チャット) | ヘイトスピーチ等での処罰 | BAN あり |
| ④ マッチメイキング制裁 | 途中抜け等の時限アクセス禁止 (例: 1時間) | 時限 BAN |

根拠:

- `Hacking` キックを長期間に何十回受けてもアカウント制裁 (時限 BAN 含む) は一度も発生していない
  → ①は接続単位のステートレスな保護で、アカウントに記録を残さないと考えられる
- ②のキックには専用のチャット告知が出るが、①では一度も出たことがない → 観測可能な挙動が異なる
- `Hacking` というラベルは catch-all で、実際には複数の検知機序が同じ表示に畳まれている

## 3. `Hacking` キックは単純なレート制限ではない

同一セッション内の比較で、**送信本数・バイト数ともに明確に重い時間帯が生き残り、
より軽い時間帯でキックされた**事例が確認できています。つまり①の中にも
「純粋な量の制限」と「パケット内容・シーケンスの形を見るヒューリスティック」があり、
後者は閾値も条件も非公開です。

また、バニラクライアントが `Hacking` キックを受けた報告は見当たらない一方、
モッドホストでは**全く同じ内容のバーストが普段は通り、ごく稀にだけキックされる**ことが観測されています。
このことから「**蹴る対象の選定は通信内容ベースだが、発火閾値はサーバー側の瞬間負荷で変動する**」
というハイブリッド型の可能性が高いと推定しています (未検証の仮説です。もし正しければ、
キックはピークタイムに偏るはずで、長期のキック時刻記録で検証できます)。

このため、**モッド側でどれだけ送信量を節制しても原理的に避けきれないキックが一定確率で存在します**。
閾値がサーバー側の事情で動くなら、再現実験が空振りし続けることとも辻褄が合います。
本モッドが自動再ホスト等の復帰機構を持っているのはこれが理由です。

## 4. 公式の一次資料

Innersloth は公式に modding 情報を公開しています:
https://github.com/Innersloth-LLC/AmongUsModdingInformation

- 1 メッセージの上限は **1200 バイト (ヘッダ込み)** と明文化されている
- 送信レートの公式値は未策定 (「バーストが必要なら modding@innersloth.com へ相談」という運用)
- host-only モッドはアンチチートが緩和されるが「常に無効化される保証はない」と明記されている

パケット関連の挙動を推測する前に、まず一次資料を参照することを推奨します。
